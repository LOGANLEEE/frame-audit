# Publishing to the Figma Community

Everything needed for the submission form, ready to paste. Assets are in `assets/`.

## Publish from the right account — do this first

**Publisher identity is effectively permanent.** Figma's docs state you cannot
transfer ownership of a plugin published outside an organization; the only route
afterwards is a discretionary Figma Support ticket. Get this right before clicking
Publish — there is no clean undo.
([Manage plugins as a developer](https://help.figma.com/hc/en-us/articles/360042293714))

Do **not** publish from a work-email account. The company can't seize the account
itself on a Professional plan (that requires Org/Enterprise with SSO), but if the
address is ever deprovisioned you lose the login — and with it the ability to
update or unpublish. What happens to a published plugin when its account dies is
not documented anywhere, so avoid the scenario rather than plan to recover from it.
A "personal team" that sits under a work-email account does not help; it's the
same account.

Setup:

1. Use (or create) a Figma account whose **primary email is personal**.
2. **Enable 2FA** on it — required to publish.
3. Desktop app → avatar menu → **Add account** (up to 10, switch without logging
   out) → sign in as the personal account.
4. Re-import the plugin under that session: **Plugins → Development → Import plugin
   from manifest…** → `manifest.json`. Development plugins appear to be registered
   per account, so re-import rather than assume it carried over.
5. Keep the plugin's source file in that account's **drafts** — never inside a
   company team, since file location constrains which profiles you may publish to.
6. At the final step, publish **as yourself** (personal profile), free.

## Flow

1. Figma menu → **Plugins → Manage plugins → Publish**.
2. Fill the four steps below (Describe → Images → Data security → Final details).
3. Submit. Review is done by a human and **can take up to two weeks**.
4. Figma generates a real plugin `id` on first publish — paste it into
   `manifest.json`, replacing `"frame-audit-dev-mode"`, and commit that change.

You can unpublish at any time (Manage resource → Unpublish). It disappears from
the Community and from users who installed it, but stays in your dev account.

Irreversible, worth repeating: the publisher identity is not freely reassignable,
a plugin published to a team profile can never be moved to another team, and
deleting an in-development plugin forfeits its likes and installs even if you
republish later.

## Assets

| Slot | File | Spec |
|------|------|------|
| Icon | `assets/icon.png` | 128×128 |
| Thumbnail | `assets/thumbnail.png` | 1920×1080 |
| Carousel (optional, up to 9) | — | Add a screenshot of the results panel on a real file |

Both were generated by `assets/make_assets.py` — rerun it after changing the name
or tagline.

> Worth adding before submitting: one screenshot of the plugin panel showing real
> findings grouped by page. Reviewers and users both respond to seeing actual
> output, and "incomplete or misleading description" is a listed rejection reason.

## Name

```
Frame Audit for Dev Mode
```

Checked against Figma's brand guidelines: the restriction covers the word "Figma"
(and fragments like "Fig") in product names — "Dev Mode" is not restricted, and
multiple published plugins use it in their titles.

## Tagline

```
Find the layers hiding outside your frames.
```

## Description

```
Dev Mode reads the node tree, not what you see on the canvas. So a screen that
looks clean can hand developers something very different: a 1594x1699 decoration
parked at x=-679 behind a 402x874 screen, invisible under Clip content, but very
much present in every measurement, every export, and every line of generated code.

Frame Audit scans your whole file and lists the layers that cause this.

What it flags
- OVERFLOW: a layer sticking out past its parent frame, tagged as clipped
  (invisible junk) or a visible spill
- GIANT: a layer more than 2x the area of its screen, reporting only the
  outermost offender rather than every wrapper around it
- HIDDEN: invisible layers still sitting in the file
- IMAGE_BLOAT: image fills whose source resolution far exceeds how they render
- BASE_SIZE: screen frames that drifted off your base device size

Findings are grouped by page with a per-rule count. Click any one to jump to that
layer and select it on canvas. Screens nested inside Sections are audited too, and
decorative pages (cover, playground, archive, scratch) are skipped so they don't
drown the real results.

Audit only - it never deletes or modifies anything. Some of those giant
decorations are load-bearing; you should look before you cut.

Fully offline. Network access is set to none, so nothing about your file ever
leaves your machine.

Open source (MIT): https://github.com/LOGANLEEE/frame-audit
```

## Category

**Design tools** (alternative: Software development — Design tools fits the
audience better, since designers run it).

## Tags

```
audit, cleanup, dev mode, handoff, qa, file hygiene, frames, overflow
```

## Data security step

- Does the plugin access data outside Figma? **No** — `networkAccess` is `none`.
- A privacy policy is not required for a plugin with no network access.
- A **support contact is required**. Use:
  `https://github.com/LOGANLEEE/frame-audit/issues`

## Rejection reasons to pre-empt

Figma's published review guidelines list these; check each before submitting.

- Crashes or obvious bugs — run `node test.cjs` (18 assertions) and run the plugin
  on at least two real files first.
- Incomplete or misleading description — the description above matches actual
  behaviour; keep them in sync if rules change.
- Recreating Figma's own functionality — this doesn't; Outline mode, `Cmd+F`, and
  deep-select all fail to surface out-of-frame content, which is the whole premise.
- Missing support contact — set, see above.
- Ads or paid-feature workarounds — none.
